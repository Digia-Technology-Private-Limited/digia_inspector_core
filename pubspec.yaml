name: digia_inspector_core
description: Core package for digia_inspector
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
